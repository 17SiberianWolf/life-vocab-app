# 07 · 测试文档

| 项 | 内容 |
|---|---|
| 文档编号 | LV-TEST-v1.0 |
| 版本 | 1.0 |
| 测试框架 | 自研轻量断言 + Node.js（无第三方测试框架） |
| 断言总数 | **212** |
| 执行结果 | **全部通过（0 失败）** |
| 执行日期 | 2026-09-10 |

---

## 1. 测试策略

### 1.1 测试分层

| 层次 | 手段 | 覆盖范围 |
|---|---|---|
| 静态校验 | Node 读取已构建的 `index.html` 做正则/JSON 断言 | 数据完整性、DOM 元素、CSS 规则、关键代码存在性 |
| 逻辑测试 | 抽取主应用 IIFE 在 Node 中 `eval` 执行 | 主 JS 语法正确性、函数可定义性 |
| 契约测试 | 校验 `data/*.json` 与数据库 schema 一致性 | 词库字段、去重、引用完整性 |
| 手工验证 | 真实浏览器（Chrome/Edge）操作 | 游戏体验、TTS、录音、云同步、PWA 安装 |

### 1.2 为什么不用 Jest / Playwright 全量自动化

| 考量 | 结论 |
|---|---|
| 项目规模 | 单文件应用，无模块打包，引入测试框架收益有限 |
| 环境依赖 | 构建产物是 HTML，直接文本断言最贴合实际运行时 |
| 沙箱限制 | 测试脚本**无法读取 `build_index.py` 源文件**，故统一改为校验已构建的 `index.html` |
| 成本 | 全量 E2E 需要浏览器驱动与云端账号，维护成本高 |

> 已识别的覆盖缺口见 §6，并登记在 [14-已知问题与风险登记册.md](./14-已知问题与风险登记册.md)。

---

## 2. 测试环境

| 项 | 版本 / 配置 |
|---|---|
| Node.js | v22.22.2 |
| 被测对象 | 已构建的 `index.html`（非源码） |
| 数据源 | `data/cards.json`、`data/topics.json`、`build-config.json` |
| 辅助模块 | `test-helpers.js`（提供 `getMainIIFEBody()` 等工具） |

### 2.1 运行方式

```bash
node test-stage2.js      # 阶段 2：基础结构
node test-stage3.js      # 阶段 3：游戏逻辑
node test-stage4.js      # 阶段 4：跟读评分
node test-stage5.js      # 阶段 5：云同步与 PWA
node test-business.js    # 阶段 2.5：商务卡集成
node test-app.js         # 阶段 1：MVP 与 5 种玩法
```

退出码：全部通过时 0；任一断言失败时输出 `!! 有 N 项失败` 并抛错。

---

## 3. 测试套件清单

| 套件 | 断言数 | 覆盖模块 | 结果 |
|---|---:|---|---|
| `test-app.js` | 33 | MVP 基础、5 个玩法、主 IIFE 启动 | ✅ 通过 |
| `test-stage2.js` | 22 | 主题/词库数据、HTML 结构、脚本块可 eval | ✅ 通过 |
| `test-business.js` | 19 | 商务主题子模块、字段完整性、浏览渲染 | ✅ 通过 |
| `test-stage3.js` | 38 | 游戏逻辑（配对/听音/连连看/消消乐）、SRS | ✅ 通过 |
| `test-stage4.js` | 34 | 跟读评分算法（编辑距离、命中矩阵、无 SR 降级） | ✅ 通过 |
| `test-stage5.js` | 66 | Supabase 配置、IndexedDB、同步层、PWA、词库规模 | ✅ 通过 |
| **合计** | **212** | | **0 失败** |

---

## 4. 需求覆盖矩阵

| 需求编号 | 需求 | 覆盖套件 | 断言示例 |
|---|---|---|---|
| FR-01 | 主题浏览与词卡 | stage2 / business | 主题存在、字段全填、subscene 渲染容器 |
| FR-02 | 碰碰乐 | app / stage3 | 回合初始化、配对判定、计时器 |
| FR-03 | 听音匹配 | app / stage3 | 候选生成、答题流转 |
| FR-04 | 连连看 | app / stage3 | 12 张翻牌、翻回逻辑 |
| FR-05 | 消消乐 | app / stage3 | 下落、生命、结算 |
| FR-06 | 跟读评分 | stage4 | `scorePronunciation` 打分、长例句 9/9 命中 89 分、无 SR 不崩 |
| FR-07 | SRS 复习 | stage3 / stage5 | 间隔序列、到期计算、每日额度 |
| FR-08 | 错题本 | stage3 | shadow 状态流转、连对 2 次移出 |
| FR-09 | 进度统计 | app | KPI 渲染 |
| FR-10 | 发音 TTS | business / stage5 | `speak()` 存在、audio 字段已剥离 |
| FR-11 | 云同步 | stage5 | 配置注入、表结构、RLS、离线队列 |
| FR-12 | PWA | stage5 | manifest、sw.js、缓存版本、图标 |
| NFR-01 | 首屏体积 | stage5 | 产物大小与 gzip 体积 |
| DR-01/02 | 词库规模 | stage5 | 3230 卡 / 25 主题 |
| DR-03 | 字段完整 | business / stage5 | en/zh/ex/ex_zh 100% |
| DR-04 | 同主题去重 | stage5 | 零重复 |

---

## 5. 关键测试用例设计

### 5.1 主 JS 语法与启动（防白屏）

| 用例 | 方法 | 期望 |
|---|---|---|
| 所有 `<script>` 块可 eval | `test-helpers.js` 提取各 script 内容逐个 `eval` | 6 个全部成功 |
| 主 IIFE 启动成功 | 在 mock DOM 环境执行主 IIFE | 不抛异常 |

> 这是最高价值的一组用例：任何语法错误都会导致整页白屏。

### 5.2 词库数据质量

| 用例 | 期望 |
|---|---|
| 卡片总数 | 3230 |
| 主题总数 | 25 |
| 唯一词条 | 2844（跨主题重复允许，如 bus 同属交通与旅行） |
| 关键字段完整性 | en / zh / ex / ex_zh / topic 均非空 |
| 同主题去重 | `(en, topic)` 组合零重复 |
| 主题引用完整性 | 卡片引用的 topic 均在 topics.json 中定义 |
| 音频字段已剥离 | 产物中无 `audio/word/*.mp3` 路径 |

### 5.3 跟读评分算法

| 用例 | 输入 | 期望 |
|---|---|---|
| 完全正确 | target == transcript | 100 分，全部 hit |
| 轻微误差 | 单复数/轻微误听（编辑距离 ≤1） | 该词 0.5 权重（near） |
| 漏词 | transcript 缺词 | 该词 0 权重（miss） |
| 多词 | transcript 多词 | 不扣分 |
| 长例句 | 9 词全中 | 89 分（漏冠词 the） |
| 无语音识别环境 | `webkitSpeechRecognition` 不存在 | `startAutoScore` 不崩溃 |

### 5.4 云同步与 PWA

| 用例 | 期望 |
|---|---|
| Supabase 配置已注入 | 产物中含 url 与 anon_key |
| 三表与 RLS 策略 | schema 文件含 3 表 + 3 条 policy |
| IndexedDB 抽象 | LVDB 暴露 get/put/getAll/del 等 API |
| 离线队列 | `syncQueue` store 存在 |
| Service Worker | `sw.js` 含 install/activate/fetch 且 `CACHE_NAME` 已 bump |
| PWA 图标 | 4 个尺寸 PNG 存在 |
| 部署产物纯净 | `dist/` 仅含 index.html + manifest + sw.js + icons |

### 5.5 商务卡集成

| 用例 | 期望 |
|---|---|
| 子模块数量 | ≥ 12 |
| 每个已声明子模块都有卡片 | 全覆盖 |
| 商务卡数量 | ≥ 158（当前 280） |
| 精编卡（id ≤ 748）必须有 tip | 148 张全部有 |
| 扩充卡 tip 可缺省 | 应用有 `c.tip ? ... : ''` 容错 |

---

## 6. 覆盖率分析（诚实评估）

### 6.1 已覆盖

- ✅ 数据完整性与一致性
- ✅ 主 JS 语法与启动（防白屏）
- ✅ 关键函数存在性
- ✅ 核心算法（编辑距离、评分、SRS 间隔）
- ✅ 配置注入正确性
- ✅ 部署产物纯净度

### 6.2 未覆盖（已知缺口）

| 缺口 | 风险 | 缓解措施 |
|---|---|---|
| **异步生命周期 / 游戏泄漏** | 高。历史 bug：离开游戏后计时器与发音继续运行 | 代码层双重防御（`stopAllGames()` + 回合身份校验）；**人工验证清单**见 §6.3 |
| 真实浏览器 E2E | 中。无法自动验证点击后的实际表现 | v0.9 阶段已用 Playwright + 真实 Edge 手工验证过注册/推送/回读/RLS 隔离 |
| 视觉回归 | 低。样式变更无自动比对 | 人工刷新确认 |
| 跨浏览器兼容 | 中。Safari 未自动化验证 | 手工抽查 |
| 并发/冲突场景 | 低。单用户应用 | 冲突策略为 last-write-wins，逻辑简单 |

### 6.3 人工回归清单（发布前必做）

| # | 步骤 | 期望 |
|---|---|---|
| 1 | 进入消消乐玩几题 → 点"主题"返回 | 不再听到发音 |
| 2 | 立即重进同一主题消消乐 | 全新一局（3 命、速度正常，无叠加加速） |
| 3 | 碰碰乐/连连看中途退出 | 无后台声音、无突然弹出的完成提示 |
| 4 | 错题本点"🔊 词""熟 ✓" | 有反应（发音 + 高亮） |
| 5 | 今日复习页点词卡按钮 | 同样有反应 |
| 6 | 断网 → 学习若干 → 恢复网络 | 数据自动补传，无报错 |
| 7 | 打开首页 | 徽标为真实待复习数（小数字，非四位数） |
| 8 | 点"添加到桌面"横幅的"稍后" | 横幅关闭 |

---

## 7. 缺陷记录与回归基线

v0.9 阶段发现并修复的缺陷，**均需作为回归基线**防止复发：

| # | 缺陷 | 根因 | 修复 | 防复发措施 |
|---|---|---|---|---|
| D-01 | 注册按钮无反应 | Tab 按钮在 `<form>` 外，`form.querySelector('.lv-tab.active')` 返回 null 抛 TypeError | 从 `.lv-auth-modal` 全局查找 + 兜底 | 输入校验 |
| D-02 | PWA 安装横幅按钮无反应、关不掉 | `install-prompt.js` 使用了 `auth-ui.js` 闭包内的 `$` | 各模块自带 `$`；安装按钮加 try/finally | 约束 DC-01 |
| D-03 | 离开游戏后仍发音、掉命、弹窗 | `show()` 无停止钩子 | `stopAllGames()` + 回合身份校验 | 约束 DC-03/DC-04 |
| D-04 | 消消乐重进后单词下落越来越快 | 旧 interval 未清除，多局叠加 | `startGravityRound()` 开头 clearInterval | 人工回归 §6.3-2 |
| D-05 | 错题本/复习页词卡按钮全失效 | 事件委托只绑在浏览页 `#cardList` | 提升到 `document` 级 | 约束 DC-02 |
| D-06 | 发布后用户看不到修复 | Service Worker cache-first 缓存旧页 | 每次发布 bump `CACHE_NAME` | 约束 DC-05 |
| D-07 | 今日复习显示 2293 | 把未学新卡计入"复习" | 只统计到期+错题；新增每日额度 | 断言已加入 stage5 |
| D-08 | 部署上传 178 个垃圾文件（含 `.git/`） | 输出目录设为仓库根 | 构建输出 `dist/`，仅 7 个文件 | stage5 校验 `dist/` 纯净 |
| D-09 | 扩充词库后 `test-business.js` 3 项失败 | 断言硬编码 158 张 / 12 模块，未随数据演进 | 改为下界断言 + 结构完整性断言 | 见 §8 |

---

## 8. 测试维护规范

### 8.1 断言编写原则

**禁止硬编码会随数据演进而变化的数值。** 应改用：

| 反例 | 正解 |
|---|---|
| `cards.length === 158` | `cards.length >= 158` |
| `subscenes.length === 12` | `subscenes.length >= 12` |
| `modules.size === 12` | 校验「已声明子模块均有卡片」 |
| 全部卡强制有 `tip` | 区分精编卡与扩充卡 |

> D-09 就是违反此原则的真实案例：词库从 158 扩到 280 后，3 条断言误报失败。修复时改为**下界 + 结构完整性**断言，后续继续加词也不会再误报。

### 8.2 新增功能的测试要求

| 变更类型 | 必须补充 |
|---|---|
| 新增主题/词库扩充 | 跑 `test-business.js` + `test-stage5.js`，确认字段完整与零重复 |
| 新增游戏 | `test-stage3.js` 补充回合初始化与结算断言 |
| 修改算法 | `test-stage4.js` 补充边界用例 |
| 修改同步层 | `test-stage5.js` 补充契约断言 |
| **任何发布** | 跑全部 6 个套件 + 人工回归清单 §6.3 + bump `CACHE_NAME` |
