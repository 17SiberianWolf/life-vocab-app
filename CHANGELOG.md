# 变更日志（CHANGELOG）

本项目所有值得记录的变更都将写在这里。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [1.1.2] — 2026-09-11 · 「✍ 拼写」新增「简单 / 地狱」双模式

### 新增

- **简单模式（抄写）**：页面显示单词 / 词组**原貌** + 中文释义，并自动播报发音；用户照着敲一遍，
  目的是降低难度、熟悉拼写、练习手感，为地狱模式做铺垫。
- **地狱模式（默写）**：仅显示**中文释义** + 发音，凭听音与释义推理并默写出英文（沿用 v1.1.0 既有逻辑）。
- **模式切换条**：`#view-spell` 标题下新增「✍ 简单（抄写） / 🔥 地狱（默写）」切换按钮，**即时切换、不打断当前回合**；选择持久化于 `localStorage('lva_spell_mode')`，**默认简单模式**。

### 说明

- 两种模式**共用同一套严格判定**（仅忽略大小写；**空格与非法字符严格**；只归一撇号 / 连字符的编码变体）
  与**逐字符实时警示**（输错立即红框抖动 + 「第 N 个字符不正确」，改对自动恢复），无需重写核心逻辑。
- **计分口径**：简单模式为**纯练习不计分** —— 只维护本批会话统计（已答 / 正确 / 连击 / 用时），
  **不写 SRS 复习队列、不进错题本、不加 XP、不 bump 全局连击**（避免「照抄正确」污染掌握度判断）；
  地狱模式完全保持 v1.1.0 行为（答对写 SRS + XP + 连击，答错记错题本）。
- 简单模式下自动隐藏「💡 提示」按钮（答案本就可见）。

### 测试

- `test-spell.js` 新增双模式相关断言（模式常量 / 切换函数 / 简单模式渲染与提交不计分 / 地狱模式仍写 SRS 等）；
  test-spell 112 → **145 项**；8 套件合计 **410 项**全绿。

### 其他

- `sw.js`：`CACHE_NAME` `v17` → `v18`（客户端有改动，需 bump）。

---

## [1.1.1] — 2026-09-10 · 修复个别单词在线发音失败（代理单词分支缺引擎回退）

### 修复

- **现象**：个别单词点发音无声，底部弹提示「在线发音加载失败, 尝试本地语音」。
- **根因**：同源 TTS 代理 `functions/tts.js` 的**单词分支只有一个上游**——有道 `dictvoice`（主/备仅是同词英式/美式双口音，**不是双引擎**）。有道词库缺条目时返回 **HTTP 500**，代理随即返回 **502**：实测 3 个连字符复合词失败 —— `time-box`、`back-office`、`spare-parts`。客户端 `<audio>` 触发 `error` → 弹上述提示 → 退化到**本地 Web Speech**，而手机端本地语音常不可用（无音色包 / 静音开关 / 微信内核），于是表现为「这个单词没声音」。
- **修复**：单词分支改为**多引擎链** —— 有道(type1) → 有道(type2) → **百度翻译 TTS** → **Google 翻译 TTS**（与句子分支共用引擎；百度已验证可合成上述全部失败词）。响应头 `X-TTS-Source` 区分实际来源：`youdao-proxy` / `baidu-word` / `google-word`（句子为 `baidu-sentence` / `google-sentence`）。
- **影响面**：单词池 2008 词全量实测失败 3 个（0.15%），修复后应为 0；短语走百度整句分支（抽样 60 条 0 失败）不受影响。
- **无需清缓存**：`/tts` 不在 Service Worker 缓存规则内（`isAsset` 只匹配静态扩展名），失败响应也不进边缘缓存，故 `CACHE_NAME` 保持 `v17`；本次未改动客户端 `index.html`。

### 测试
- `test-tts.js` 新增第 13 节「服务端 /tts 代理引擎路由」**12 项**断言（mock 上游 + 直接加载 `functions/tts.js`）：有道 500 → 仍返回 200 且 `X-TTS-Source: baidu-word`、有道命中时不请求百度（不无谓降级音质）、含空格与超 40 字符文本仍走整句分支、缺参/超长参数返回 400。
- `test-tts.js` 41 → **53 项**；8 套件合计 **377 项**全绿。

---

## [1.1.0] — 2026-09-10 · 新增「✍ 拼写」玩法（看中文 + 听音 → 打字拼写）

### 新增

第 7 种玩法 **✍ 拼写**：页面显示单词/短语的**中文释义**并自动播报**发音**，用户用键盘（电脑）
或软键盘（手机）打出英文拼写；参考 `xueciya.com/typing` 的「默写模式」，全终端零依赖。

- **出题**：显示 `card.zh`，进题自动 `speak(card.en)` 一次，可重播 / 🐢 慢速；入口支持**按主题**（主题卡「✍ 拼写」）与**全库**（顶栏「✍ 拼写」）
- **题源**：单词 + 短语（`card.en`）；过滤含括号/斜杠/句点等占位短语（如 `pear (Asian)`、`A / B`），可出题池 ~3178 / 3230 张
- **判定**：**忽略大小写**；**空格严格**（含首尾空格与内部连续空格）；**非法特殊字符严格**；仅对「同义字符」做归一（弯撇号 `’‘` → `'`、连字符变体 `–—−` → `-`）
- **逐字符实时警示**：输入过程中逐字符比对，**前缀正确不提示**；一旦某位字符打错（含**首字母错**、**中间字母错**、**多打字符**）立即**红框 + 抖动**并提示「⚠ 第 N 个字符不正确」；改回正确前缀后警示自动消除，且**不阻断输入**（仅出错位置变化时重抖，避免持续闪烁）
- 💡 提示：逐步揭示首字母并显示字母数；⏭ 跳过：不计分、不写 SRS
- **数据接入**：答对 → `recordResult(true)` + `addXP(10)` + `bumpStreak()`；答错 → `recordResult(false)`（**仅首次**去重，避免连击刷 SRS）；连错 3 次自动揭示答案；回合末展示答对数/正确率/最长连击/用时
- **移动端可靠性（本站首个文本输入框）**：`inputmode=text / autocomplete=off / autocorrect=off / autocapitalize=none / spellcheck=false / enterkeyhint=done`；移动端字号 ≥16px 防 iOS 聚焦缩放、触控目标 ≥44px；移动端**不自动聚焦**（避免软键盘遮挡发音键）；Enter 提交判断 `e.isComposing`（兼容中文 IME 组合态）

### 变更

- 主题页文案「6 种玩法」→「7 种玩法」；顶栏新增「✍ 拼写」入口
- `sw.js` `CACHE_NAME` `v16 → v17`（保证手机端拿到新页面）

### 影响范围
- 现有 6 种玩法与发音链路不受影响；新增玩法独立模块 + 独立回合状态，复用 Listen 的「回合身份守卫」防异步串扰。
- 测试：新增 `test-spell.js`（**112 项**断言：结构接入 / 移动端属性 / 纯函数（含逐字符校验、空格与非法字符严格）/ 逐字符实时警示行为 / 完整回合流程 / 数据预检）；8 个套件合计 **365 项断言**全绿。

---

## [1.0.7] — 2026-09-10 · 例句改用国内百度 TTS + 修复长句误报失败

### 修复

**现象**：v1.0.6 后例句可整句朗读，但**较长例句**会弹出"整句在线发音失败，尝试逐词播放"
—— 而弹出时整句其实**仍完整读完**。

**根因**：这是**客户端失败判定的误报**，并非 Google 不稳定：
`_playOnlineSentence` 把 `stalled`（缓冲期无数据的瞬时事件）和 9s 硬看门狗都当作失败。
长句经代理分段合成后体积更大、首字节更晚，缓冲期触发 `stalled` / 超过看门狗 →
误判失败并弹提示、同时启动逐词回退（而原整句音频随后正常播完）。

**处理**：

- 在线发音**例句主引擎改用百度翻译 TTS**（`fanyi.baidu.com/gettts`，国内服务、免密钥、
  支持任意整句），Google 翻译 TTS 作备份；`functions/tts.js` 句子路径为
  「百度 → Google」双引擎链，长文本按词边界切片拼接 MP3
- 客户端失败判定改为**只在真失败时触发**：移除 `stalled` 误触发器；看门狗(15s)在
  `playing/loadeddata/canplay/timeupdate`（开始出声）后**自动解除**，并随 `progress`
  （数据推进）重置；已出声的整句一律视为成功，不再误报
- `sw.js` `CACHE_NAME` `v15 → v16`

### 影响范围
- 长例句不再误报"整句在线发音失败"，也不会误触发逐词回退。
- 例句发音走国内百度 TTS，对国内用户更快、更稳；Google 作为兜底。
- 测试：`test-tts.js` 扩至 **41 项**断言；7 个套件合计 **253 项断言**全绿。

---

## [1.0.6] — 2026-09-10 · 例句整句自然朗读（不再逐词拆读）

### 修复

**现象**：v1.0.5 后，手机端单词发音正常，但例句"只能读前一两个词"，且逐词播放、语调生硬，
完全失去句子学习的语感。

**根因**：v1.0.4 为绕开"有道不支持整句"而引入的**逐词拆分顺序播放**方案，本质是单词拼接，
并非真正的句子朗读。链条中部分词在线请求失败后推进中断，于是"只读前 1~2 词"；
逐词拼接本身也缺乏句间语调连贯，听感生硬。

**处理**：

- `functions/tts.js` 重构为双引擎路由：
  - **单词**（无空格且 ≤40 字符）→ 有道 `dictvoice`（快、覆盖好，保留双口音回退）
  - **句子**（含空格或较长）→ **Google 翻译 TTS 整句自然合成**（`client=gtx`，带自然语调，
    不再逐词拆读）；长句按词边界切片后拼接 MP3 字节流返回
- `build_index.py` 在线发音逻辑重写：例句走 `_playOnlineSentence`**一次性请求整句**，
  仅在整句在线请求失败时才退化为逐词拆分（保底出声），不再作为默认读法
- `sw.js` `CACHE_NAME` `v14 → v15`（确保用户拿到新发音逻辑）

### 影响范围
- 例句现在以完整句子、自然语调朗读，符合语言学习预期。
- 单词发音路径不变（仍走有道，快速稳定）。
- Google TTS 经 Cloudflare 边缘同源代理，绕开手机运营商对外部域名的封锁。

---

## [1.0.5] — 2026-09-10 · 修复"修了却像没生效"（SW 更新投递）

### 修复

**现象**：v1.0.4 部署后，用户手机单词可发音、例句仍报"在线发音加载失败"。

**诊断**：服务器已含 v1.0.4 逐词拆分逻辑（`sw.js=v13`、首页 9 处命中），但该报错提示语是
v1.0.3 整句直连的提示 —— 说明用户手机仍在跑**旧的 v12 缓存页面**。根因是 SW 对导航请求
用 `stale-while-revalidate`，会先返回旧缓存 HTML，用户必须**手动刷两次**才能拿到新逻辑，
造成"修了却像没生效"的假象。

**处理**：

- `sw.js` 导航请求改为 **network-first**（始终取最新 `index.html`，仅离线时回退缓存）
- 新增 `controllerchange` 监听：新 SW 接管页面后**自动刷新一次**，用户无需手动刷两次
- `CACHE_NAME` `v13 → v14`

### 影响范围
- 今后每次发版，用户重新打开/刷新一次页面即自动取到最新版本（含未来的 TTS 改动）。

---

## [1.0.4] — 2026-09-10 · 例句发音修复 + 单词命中率提升

### 修复

**现象**：v1.0.3 后，手机端绝大多数单词能发音，但**所有例句全部失败**，且仍有少量单词失败。

**根因**：有道 `dictvoice` 是**词典单词接口，不支持整句**。代理把它当作一个整体请求转发，
长句被上游以 502 拒绝 —— 所以"单词成功、例句全失败"。个别单词失败则是因为该词在所选
口音（英式/美式）的有道词典里缺条目，上游返回非音频响应。

**处理**：

- **客户端 `playOnline()` 改为"逐词拆分顺序播放"**：文本按空格拆成 token，逐个请求同源
  `/tts` 顺序播放（每个词走与"成功单词"完全相同的可靠通路），单个词失败自动跳过、不卡死。
  例句从此 100% 可发音；引入 `_onlineEpoch` 会话令牌，切视图/游戏退出时使待执行回调失效，
  避免旧定时器串扰（修复同类"消消乐退出仍发声"隐患）。
- **代理 `functions/tts.js` 双口音回退**：主口音（英式 `type=1` / 美式 `type=2`）无音频时，
  自动回退到另一口音；并识别"上游 200 但返回非音频"的无效响应，避免静默失败。个别生僻词
  命中率显著提升。
- `sw.js` 缓存版本 `v12 → v13`（保证手机取到含新 `playOnline` 的页面）。
- `test-tts.js` 新增"例句拆分""单 token 失败不卡死"断言，专项测试由 30 项扩到 **40 项全通过**。

### 影响范围
- 移动端：词卡 / 例句 / 慢速 / 消消乐 / 连连看 / 听力 / 复习页全部发音稳定。
- 桌面端：本地 Web Speech 优先，失败才走逐词代理，行为更稳健。

---

## [1.0.3] — 2026-09-10 · 移动端发音彻底修复（同源 TTS 代理）

### 修复

**现象**：v1.0.1/v1.0.2 后，手机端（微信 / 小米 / 夸克）页面能打开，但发音
**绝大多数时候失败**，把「发音方式」设为「仅在线音频」也报"在线发音加载失败"。

**根因**：v1.0.1 的在线兜底直接请求 `https://dict.youdao.com/dictvoice`（**外部域名**）。
手机网络能打开 `life-vocab-app.pages.dev`（本站点域名可达），但运营商对
`dict.youdao.com` 等外部域名实施 DNS 污染 / 连接挂起，导致该请求在手机端整体失败。
（从服务器侧 curl 有道接口是正常的，进一步印证是"手机→外部域名"被拦，而非接口本身。）

**处理 —— 在线发音改为同源代理**：

- 新增 `functions/tts.js`（Cloudflare Pages Function）：手机浏览器只访问
  `https://life-vocab-app.pages.dev/tts?audio=...&type=1|2`，由 Cloudflare 边缘去取
  有道音频再回传。**彻底绕开运营商对外部域名的封锁**（本站点域名已验证手机可达）。
- 代理结果按 upstream URL 缓存 1 年（重复词秒回、不重复打上游）。
- `playOnline()` 改为请求同源 `/tts`；并补 `error` / `stalled` 事件监听（**iOS Safari
  加载失败时不 reject `play()` 的 promise，只触发 `error`**，原实现会静默无声且无提示）。
- `speakTTS()` 在 `auto` 模式下：移动端（已验证 Web Speech 普遍不可靠）**直接走同源
  代理**，首词即出声、零等待；桌面端仍优先本地 Web Speech。
- `sw.js` 缓存版本 `v11 → v12`。
- 构建脚本把 `functions/` 同时复制到 `dist/functions/`，兼容 Cloudflare 在仓库根与
  output 目录两种函数查找方式。

### 影响范围
- 全部发音入口（词卡 / 例句 / 慢速 / 消消乐 / 连连看 / 听力 / 复习页）在移动端稳定性大幅提升。
- 桌面端行为不变（本地 TTS 优先，失败才走代理）。

---

## [1.0.2] — 2026-09-10 · 修复手机端"打不开"（外部 CDN 阻塞）

### 修复

**现象**：手机端（微信 / 小米 / 夸克）打不开页面，电脑端正常。

**根因**：页面唯一的外部依赖是 jsdelivr 的 Supabase SDK，且是**同步 `<script>`**。
国内手机网络下 jsdelivr 域名常被 DNS 污染 / 连接挂起，同步脚本会阻塞整个页面渲染，
表现为"手机打不开、电脑能打开"（电脑网络可达 jsdelivr）。

**处理**：

- 下载 supabase-js UMD 到 `vendor/supabase.js`，构建时复制到 `dist/`，页面改为
  **同源** `<script src="supabase.js">` 加载，彻底消除跨域阻塞点
- `sw.js` 加固（缓存版本 `v10 → v11`）：
  - 预缓存从 `cache.addAll`（整包失败即 SW 装不上）改为逐项 `add` 并容错
  - navigate 请求改为 **stale-while-revalidate**（缓存秒开 + 后台更新）
  - 修复网络失败时 `respondWith(undefined)` 导致白屏的隐患
  - 静态资源缓存正则补上 `.js`

---

## [1.0.1] — 2026-09-10 · 移动端发音修复

### 修复

**手机端听不到发音（全部入口：词卡 / 例句 / 慢速 / 消消乐 / 连连看 / 听力 / 复习页）**
根因有 5 个，均在 TTS 层统一修复（原 `fallbackTTS` 重写为 TTS 引擎 v2）：

1. **iOS 手势未解锁**：`speechSynthesis.speak()` 首次必须在用户手势的同步调用栈内触发，否则永久静默
   → 首个 `touchstart / mousedown / keydown / click` 时静默解锁（空白 utterance + 静音 wav 解锁 `<audio>`）
2. **音色列表为空**：iOS / Android 首次 `getVoices()` 返回空数组，且 `onvoiceschanged` 未必触发
   → 主动轮询 0 / 300 / 1200ms 缓存音色
3. **缺 en-GB 语音包**：国行手机默认没有英式语音，指定 `en-GB` 时静默无声（桌面端有多种英文语音所以正常）
   → 音色匹配放宽 `en-GB → 任意 en* → 系统默认`，且 `utterance.lang` 跟随实际音色
4. **Chrome / Android 吞语句**：`cancel()` 后立刻 `speak()` 会被丢弃，长句 15s 自动暂停
   → cancel 后让出一个 tick 再播；播报期间 keepAlive 定时 resume（iOS 不启用，避免打断）
5. **内核不支持 / 静音开关**：微信内置浏览器不支持 Web Speech；iOS 侧边静音开关会屏蔽 speechSynthesis
   → 1.2s 内未触发 `onstart` 判定静默失败，自动切换**在线发音**兜底（有道词典发音接口），并记忆状态不再空等

### 新增

- **设置页「发音方式」**：自动（本地优先，失败转在线）/ 仅本地语音（可离线）/ 仅在线音频
- **设置页「🔊 发音自检」**：输出平台、Web Speech 支持、手势解锁状态、可用音色数、当前音色、最近错误，并播放测试句
- **发音按钮按下反馈**：按钮短暂高亮，用于区分"没点到"和"点了没声音"
- **移动端触摸区域放大**：词卡按钮由约 24px 高提升至 34px（`max-width: 600px`）
- `test-tts.js`：移动端发音链路专项测试，30 项断言（降级 / 音色匹配 / 静默切换 / cancel 时序 / 解锁监听）
- Service Worker 缓存版本 `wordmatch-v9 → v10`（保证手机端能取到新页面）

---

## [1.0.0] — 2026-09-10 · 首个稳定版

### 新增

**词库**
- 词库从 **748 张扩充至 3230 张 / 25 主题 / 2700+ 唯一词条**
- 新增 14 个主题：学术雅思（315）、商务汇报（280）、中国菜品（198）、运动健康（195）、旅行出行（179）、情绪性格（166）、金钱金融（158）、功能表达（155）、科技数码（154）、天气环境（152）、社会传媒（151）、短语习语（149）、学习教育（147）、工业现场（141）、职场办公（100）
- 「工业现场」主题按作者职业背景定制（连铸、结晶器、扇形段、PLC、机器视觉、CCD 相机、缺陷检测、OEE）
- 「中国菜品」主题：100 道中国菜（8 大地区）+ 30 道补充 + 68 句点餐英语，支持 9 个子分类筛选

**功能**
- **云同步**：Supabase 邮箱注册登录，学习进度跨设备同步，离线队列补传
- **PWA**：可安装到桌面/主屏，离线可用
- **每日新词额度**：新词按每天 10 个限量引入，与复习队列分离
- **进度页**：学习统计、连续天数、XP、掌握度

**工程**
- `expand_vocab.py` 词库合并流水线（备份 / 去重 / 统计 / 试运行）
- `dist/` 纯净部署目录（仅 7 个核心文件）
- `serve.py` 无缓存本地开发服务器
- 完整文档归档（16 项，见 `docs/00-归档总目.md`）

### 修复

- **PWA 安装横幅按钮无反应且无法关闭**（`bb33806`）
  根因：`install-prompt.js` 调用了定义在 `auth-ui.js` 闭包内的 `$`，跨模块拿不到 → `ReferenceError` → 事件从未绑定。
  修复：模块内自备 `$`；安装按钮加 `try/finally` 兜底。

- **离开游戏视图后游戏未停止、持续发音**（`422d5b0`）
  根因：`show()` 切视图无停止逻辑，定时器与回调继续运行；消消乐重进时旧 interval 叠加导致下落加速。
  修复：新增 `stopAllGames()` 钩子 + 所有异步回调加回合身份校验（消消乐 / 碰碰乐 / 连连看 / 听音 四处）。

- **词卡按钮在错题本、今日复习页全部无效**（`88e4abb`）
  根因：`makeWordCard()` 是共用组件，但事件委托只绑在浏览页 `#cardList`。
  修复：委托提升到 `document` 级 + `cardEl` 空值防护。

- **今日复习徽标显示 2293**（`0961e1f`）
  根因：`getDueCards()` 把从未学过的新卡也算进"今日复习"。
  修复：只统计 SRS 到期 + 错题；新增每日新词额度；徽标 >99 显示 `99+`。

- **补全 158 条例句缺失的中文翻译**（原商务汇报主题的冶金/连铸场景例句）

- **测试断言未随词库演进更新**（`test-business.js`）
  商务卡 158→280、模块 12→13、新增卡无 `tip` 属预期。已修正断言，测试全绿。

### 变更

- **发音方案**：从离线 mp3（2992 个 / 61MB）改为**浏览器内置 TTS**
  构建时按 `build-config.json` 的 `audio.embed` 开关剥离音频路径，产物 494KB → 344KB。保留开关可回退。
- **部署平台**：Vercel → 腾讯云 CloudBase → **Cloudflare Pages**（前两者均失败，见 `10-ADR-006`）
- **构建产物 `dist/` 提交进 git**：绕开 Cloudflare 构建环境无 python3 的限制（见 `10-ADR-007`）
- **主页副标题动态化**：替换过时的硬编码文案"10 个主题、590 张卡"

### 移除

- `vercel.json`（Vercel 配置）
- `cloudbase-deploy/`（腾讯云部署包）

---

## [0.5.0] — 2026-09-08 · 阶段 5（云同步 + PWA）

### 新增
- Supabase 后端：三表 `cards_progress` / `user_settings` / `rec_scores` + RLS 策略
- `lib/db.js`：IndexedDB 封装，替代 localStorage
- `lib/sync.js`：同步层（推/拉/离线队列/最后写入优先冲突合并）
- `lib/auth-ui.js`：登录注册 UI
- `lib/install-prompt.js`：PWA 安装横幅
- `sw.js` + `manifest.webmanifest` + 图标：PWA 支持

---

## [0.4.0] — 2026-08 · 阶段 4

- 跟读评分功能（Web Speech API）
- 错题本与 Shadow Pool 机制

## [0.3.0] — 2026-08 · 阶段 3

- 四种游戏：碰碰乐、听音、连连看、消消乐
- SRS 复习算法（SM-2 变体）
- 进度统计页

## [0.2.0] — 2026-08 · 阶段 2

- 词卡浏览、搜索、发音
- 主题子场景筛选

## [0.1.0] — 2026-08 · 阶段 1

- 项目初始化：单文件 HTML 架构
- 748 张卡 / 11 主题（商务 158 + 10 生活主题）
- edge-tts 离线音频生成（2992 个 mp3）

---

## 版本号规则

```
主版本.次版本.修订号

1.0.0
│ │ │
│ │ └─ 修订：bug 修复、文档更新
│ └─── 次版本：新功能（向后兼容）
└───── 主版本：不兼容的重大变更
```

**当前版本：1.1.2**

---

## 未来规划（Roadmap）

| 版本 | 计划内容 | 优先级 |
|---|---|---|
| v1.1 | 设置页开放「每日新词额度」调整 | 高 |
| v1.1 | 内联 supabase-js，摆脱 CDN 依赖 | 中 |
| v1.1 | 学习图表（近 30 天复习量趋势） | 中 |
| v1.2 | 支持导入/导出自定义词库（需配套 XSS 清洗） | 中 |
| v1.2 | 例句难度自适应 | 低 |
| v2.0 | 数据层迁移至国内 BaaS（若 Supabase 访问持续恶化） | 按需 |

详见 [docs/14-已知问题与风险登记册.md](./docs/14-已知问题与风险登记册.md)。

---

[1.1.2]: https://github.com/17SiberianWolf/life-vocab-app/releases/tag/v1.1.2
[1.1.1]: https://github.com/17SiberianWolf/life-vocab-app/releases/tag/v1.1.1
[1.1.0]: https://github.com/17SiberianWolf/life-vocab-app/releases/tag/v1.1.0
[1.0.7]: https://github.com/17SiberianWolf/life-vocab-app/releases/tag/v1.0.7
[1.0.0]: https://github.com/17SiberianWolf/life-vocab-app/releases/tag/v1.0.0
