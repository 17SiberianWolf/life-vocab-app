# 变更日志（CHANGELOG）

本项目所有值得记录的变更都将写在这里。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [1.0.2] — 2026-09-10 · 修复手机端"打不开"（外部 CDN 阻塞）

### 修复

**现象**：手机端（微信 / 小米 / 夸克）打不开页面，电脑端正常。

**根因**：页面唯一的外部依赖是 jsdelivr 的 Supabase SDK，且是**同步 `<script>`**。
国内手机网络下 jsdelivr 域名常被 DNS 污染 / 连接挂起，同步脚本会阻塞整个页面渲染，
表现为"手机打不开、电脑能打开"（电脑网络可达 jsdelivr）。

**处理**：

- 下载 supabase-js UMD 到 `vendor/supabase.js`，构建时复制到 `dist/`，页面改为
  **同源** `<script src="supabase.js">` 加载，彻底消除跨域阻塞点
- `sw.js` 加固（缓存版本 `v10 → v11`）：
  - 预缓存从 `cache.addAll`（整包失败即 SW 装不上）改为逐项 `add` 并容错
  - navigate 请求改为 **stale-while-revalidate**（缓存秒开 + 后台更新）
  - 修复网络失败时 `respondWith(undefined)` 导致白屏的隐患
  - 静态资源缓存正则补上 `.js`

---

## [1.0.1] — 2026-09-10 · 移动端发音修复

### 修复

**手机端听不到发音（全部入口：词卡 / 例句 / 慢速 / 消消乐 / 连连看 / 听力 / 复习页）**
根因有 5 个，均在 TTS 层统一修复（原 `fallbackTTS` 重写为 TTS 引擎 v2）：

1. **iOS 手势未解锁**：`speechSynthesis.speak()` 首次必须在用户手势的同步调用栈内触发，否则永久静默
   → 首个 `touchstart / mousedown / keydown / click` 时静默解锁（空白 utterance + 静音 wav 解锁 `<audio>`）
2. **音色列表为空**：iOS / Android 首次 `getVoices()` 返回空数组，且 `onvoiceschanged` 未必触发
   → 主动轮询 0 / 300 / 1200ms 缓存音色
3. **缺 en-GB 语音包**：国行手机默认没有英式语音，指定 `en-GB` 时静默无声（桌面端有多种英文语音所以正常）
   → 音色匹配放宽 `en-GB → 任意 en* → 系统默认`，且 `utterance.lang` 跟随实际音色
4. **Chrome / Android 吞语句**：`cancel()` 后立刻 `speak()` 会被丢弃，长句 15s 自动暂停
   → cancel 后让出一个 tick 再播；播报期间 keepAlive 定时 resume（iOS 不启用，避免打断）
5. **内核不支持 / 静音开关**：微信内置浏览器不支持 Web Speech；iOS 侧边静音开关会屏蔽 speechSynthesis
   → 1.2s 内未触发 `onstart` 判定静默失败，自动切换**在线发音**兜底（有道词典发音接口），并记忆状态不再空等

### 新增

- **设置页「发音方式」**：自动（本地优先，失败转在线）/ 仅本地语音（可离线）/ 仅在线音频
- **设置页「🔊 发音自检」**：输出平台、Web Speech 支持、手势解锁状态、可用音色数、当前音色、最近错误，并播放测试句
- **发音按钮按下反馈**：按钮短暂高亮，用于区分"没点到"和"点了没声音"
- **移动端触摸区域放大**：词卡按钮由约 24px 高提升至 34px（`max-width: 600px`）
- `test-tts.js`：移动端发音链路专项测试，30 项断言（降级 / 音色匹配 / 静默切换 / cancel 时序 / 解锁监听）
- Service Worker 缓存版本 `wordmatch-v9 → v10`（保证手机端能取到新页面）

---

## [1.0.0] — 2026-09-10 · 首个稳定版

### 新增

**词库**
- 词库从 **748 张扩充至 3230 张 / 25 主题 / 2700+ 唯一词条**
- 新增 14 个主题：学术雅思（315）、商务汇报（280）、中国菜品（198）、运动健康（195）、旅行出行（179）、情绪性格（166）、金钱金融（158）、功能表达（155）、科技数码（154）、天气环境（152）、社会传媒（151）、短语习语（149）、学习教育（147）、工业现场（141）、职场办公（100）
- 「工业现场」主题按作者职业背景定制（连铸、结晶器、扇形段、PLC、机器视觉、CCD 相机、缺陷检测、OEE）
- 「中国菜品」主题：100 道中国菜（8 大地区）+ 30 道补充 + 68 句点餐英语，支持 9 个子分类筛选

**功能**
- **云同步**：Supabase 邮箱注册登录，学习进度跨设备同步，离线队列补传
- **PWA**：可安装到桌面/主屏，离线可用
- **每日新词额度**：新词按每天 10 个限量引入，与复习队列分离
- **进度页**：学习统计、连续天数、XP、掌握度

**工程**
- `expand_vocab.py` 词库合并流水线（备份 / 去重 / 统计 / 试运行）
- `dist/` 纯净部署目录（仅 7 个核心文件）
- `serve.py` 无缓存本地开发服务器
- 完整文档归档（16 项，见 `docs/00-归档总目.md`）

### 修复

- **PWA 安装横幅按钮无反应且无法关闭**（`bb33806`）
  根因：`install-prompt.js` 调用了定义在 `auth-ui.js` 闭包内的 `$`，跨模块拿不到 → `ReferenceError` → 事件从未绑定。
  修复：模块内自备 `$`；安装按钮加 `try/finally` 兜底。

- **离开游戏视图后游戏未停止、持续发音**（`422d5b0`）
  根因：`show()` 切视图无停止逻辑，定时器与回调继续运行；消消乐重进时旧 interval 叠加导致下落加速。
  修复：新增 `stopAllGames()` 钩子 + 所有异步回调加回合身份校验（消消乐 / 碰碰乐 / 连连看 / 听音 四处）。

- **词卡按钮在错题本、今日复习页全部无效**（`88e4abb`）
  根因：`makeWordCard()` 是共用组件，但事件委托只绑在浏览页 `#cardList`。
  修复：委托提升到 `document` 级 + `cardEl` 空值防护。

- **今日复习徽标显示 2293**（`0961e1f`）
  根因：`getDueCards()` 把从未学过的新卡也算进"今日复习"。
  修复：只统计 SRS 到期 + 错题；新增每日新词额度；徽标 >99 显示 `99+`。

- **补全 158 条例句缺失的中文翻译**（原商务汇报主题的冶金/连铸场景例句）

- **测试断言未随词库演进更新**（`test-business.js`）
  商务卡 158→280、模块 12→13、新增卡无 `tip` 属预期。已修正断言，测试全绿。

### 变更

- **发音方案**：从离线 mp3（2992 个 / 61MB）改为**浏览器内置 TTS**
  构建时按 `build-config.json` 的 `audio.embed` 开关剥离音频路径，产物 494KB → 344KB。保留开关可回退。
- **部署平台**：Vercel → 腾讯云 CloudBase → **Cloudflare Pages**（前两者均失败，见 `10-ADR-006`）
- **构建产物 `dist/` 提交进 git**：绕开 Cloudflare 构建环境无 python3 的限制（见 `10-ADR-007`）
- **主页副标题动态化**：替换过时的硬编码文案"10 个主题、590 张卡"

### 移除

- `vercel.json`（Vercel 配置）
- `cloudbase-deploy/`（腾讯云部署包）

---

## [0.5.0] — 2026-09-08 · 阶段 5（云同步 + PWA）

### 新增
- Supabase 后端：三表 `cards_progress` / `user_settings` / `rec_scores` + RLS 策略
- `lib/db.js`：IndexedDB 封装，替代 localStorage
- `lib/sync.js`：同步层（推/拉/离线队列/最后写入优先冲突合并）
- `lib/auth-ui.js`：登录注册 UI
- `lib/install-prompt.js`：PWA 安装横幅
- `sw.js` + `manifest.webmanifest` + 图标：PWA 支持

---

## [0.4.0] — 2026-08 · 阶段 4

- 跟读评分功能（Web Speech API）
- 错题本与 Shadow Pool 机制

## [0.3.0] — 2026-08 · 阶段 3

- 四种游戏：碰碰乐、听音、连连看、消消乐
- SRS 复习算法（SM-2 变体）
- 进度统计页

## [0.2.0] — 2026-08 · 阶段 2

- 词卡浏览、搜索、发音
- 主题子场景筛选

## [0.1.0] — 2026-08 · 阶段 1

- 项目初始化：单文件 HTML 架构
- 748 张卡 / 11 主题（商务 158 + 10 生活主题）
- edge-tts 离线音频生成（2992 个 mp3）

---

## 版本号规则

```
主版本.次版本.修订号

1.0.0
│ │ │
│ │ └─ 修订：bug 修复、文档更新
│ └─── 次版本：新功能（向后兼容）
└───── 主版本：不兼容的重大变更
```

**当前版本：1.0.0**

---

## 未来规划（Roadmap）

| 版本 | 计划内容 | 优先级 |
|---|---|---|
| v1.1 | 设置页开放「每日新词额度」调整 | 高 |
| v1.1 | 内联 supabase-js，摆脱 CDN 依赖 | 中 |
| v1.1 | 学习图表（近 30 天复习量趋势） | 中 |
| v1.2 | 支持导入/导出自定义词库（需配套 XSS 清洗） | 中 |
| v1.2 | 例句难度自适应 | 低 |
| v2.0 | 数据层迁移至国内 BaaS（若 Supabase 访问持续恶化） | 按需 |

详见 [docs/14-已知问题与风险登记册.md](./docs/14-已知问题与风险登记册.md)。

---

[1.0.0]: https://github.com/17SiberianWolf/life-vocab-app/releases/tag/v1.0.0
