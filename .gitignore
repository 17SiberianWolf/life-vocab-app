# Build artifacts
*.pyc
__pycache__/

# Dependencies
node_modules/
venv/
env/

# Environment & secrets
.env
.env.local
.env.*.local
!.env.example

# OS / editors
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*

# Test artifacts
test-results/
coverage/
.nyc_output/

# Audio (生成产物,可重新生成)
audio/

# 数据合并前的自动备份 (本地保留即可, 不入 git)
data/backup/

# 纯净部署目录 (构建产物, 提交进 git 供 Cloudflare Pages 直接部署, 无需构建环境)
# dist/ 不再排除: 已提交, 部署时 Build command 留空 + Output directory=dist

# Supabase local config (如果有)
supabase/.temp/

# 临时文件
*.tmp
index.html.tmp
